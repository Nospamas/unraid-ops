---
title: Tower
theme: dark
color: slate
headerStyle: clean
useEqualHeights: true
disableCollapse: true
# Groups cut by use vs machinery rather than by subject (ticket 39). The old
# Media group held plex beside the five services that feed it, so nothing on
# the page said which one you actually wanted. Books dissolved: calibre is
# something you open, lazylibrarian is something that fetches.
layout:
  "Watch & Read":
    style: row
    columns: 3
    icon: mdi-play-circle
  Acquire:
    style: row
    columns: 3
    icon: mdi-magnify
  Transport:
    style: row
    columns: 3
    icon: mdi-download
  Infrastructure:
    style: row
    columns: 3
    icon: mdi-server
