# Decrypted secrets. The encrypted *.sops.env files are committed; these are not.
secrets.env
**/secrets.env

# The age private key. Repo root, found by SOPS_AGE_KEY_FILE in .mise.toml.
age.key

.claude/
